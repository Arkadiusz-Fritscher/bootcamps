export default async function (context) {
  // const token = context.$strapi.getToken();
  // if (token) {
  //   if (!context.store.getters.getStrapiToken) {
  //     context.store.commit('setStrapiToken', token);
  //   }
  //   if (Object.keys(context.store.getters.getUser).length === 0) {
  //     await getUser();
  //   }
  // }
  // async function getUser() {
  //   const user = await context.$strapi.fetchUser();
  //   context.store.commit('setUser', user);
  //   return user;
  // }
}
